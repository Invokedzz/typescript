export class Egyptshit {
    constructor (
        private element: string,
    ) {};

    public letsBuildit (): void {
        let limit = 20;
        for (let weloveVasco = 1; weloveVasco <= limit; weloveVasco += 1) {
            let paranoidAndroid = ' '.repeat(limit - weloveVasco);
            let darkStar = '*'.repeat(weloveVasco * 2 - 1);
            let agroupThem = paranoidAndroid + darkStar;
            console.log(agroupThem);
        }
    };

    public creatingtheInverse () {
        let limitBelow = 20;
        for (let wehateVasco = limitBelow; wehateVasco >= 0; wehateVasco -= 1) {
            let noSurprises = ' '.repeat(limitBelow - wehateVasco);
            let plasticTrees = '*'.repeat(wehateVasco * 2 - 1);
            let agroupAgain = noSurprises + plasticTrees;
            console.log(agroupAgain);
        }
    }

};

const triangleIlluminati = new Egyptshit('');
triangleIlluminati.letsBuildit();
triangleIlluminati.creatingtheInverse();